# Задачи

- [x] Добавить модель `behavior_metrics` с CRUD (upsert по session_id) и Pydantic-схемы.
- [x] Добавить роутер: публичный `POST /api/behavior-metrics/` и JWT-защищённый `GET /api/behavior-metrics/stats`, подключить в `main.py`.
- [x] Добавить backend-тесты: upsert, публичность POST, 401 на stats без токена, агрегаты.
- [x] Добавить модуль сбора метрик на главной странице (время, клики, курсор раз в секунду, POST раз в секунду).
- [x] Добавить кнопку «Статистика» и полноэкранное модальное окно с периодами и canvas-хитмэпом.
- [x] Собрать образы, задеплоить, выполнить smoke-проверки через Nginx.
- [x] Обновить README, technical_overview, changelog и отметить задачи выполненными.
